import type { Command } from 'commander';

export function registerTocCommands(_program: Command): void {
  // Placeholder — the toc command group is implemented against the surface
  // locked in tests/spec-coverage.test.ts.
}
