import type { Command } from 'commander';

export function registerAuthCommands(_program: Command): void {
  // Placeholder — the auth command group is implemented against the surface
  // locked in tests/spec-coverage.test.ts.
}
