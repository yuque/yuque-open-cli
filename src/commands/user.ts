import type { Command } from 'commander';

export function registerUserCommands(_program: Command): void {
  // Placeholder — the user command group is implemented against the surface
  // locked in tests/spec-coverage.test.ts.
}
