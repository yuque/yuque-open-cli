import type { Command } from 'commander';

export function registerRepoCommands(_program: Command): void {
  // Placeholder — the repo command group is implemented against the surface
  // locked in tests/spec-coverage.test.ts.
}
