import type { Command } from 'commander';

export function registerGroupCommands(_program: Command): void {
  // Placeholder — the group command group is implemented against the surface
  // locked in tests/spec-coverage.test.ts.
}
