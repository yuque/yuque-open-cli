import type { Command } from 'commander';

export function registerDocCommands(_program: Command): void {
  // Placeholder — the doc command group is implemented against the surface
  // locked in tests/spec-coverage.test.ts.
}
