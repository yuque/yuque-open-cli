import type { Command } from 'commander';

export function registerStatsCommands(_program: Command): void {
  // Placeholder — the stats command group is implemented against the surface
  // locked in tests/spec-coverage.test.ts.
}
